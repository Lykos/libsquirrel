#ifndef TREAPBASEITERATOR_H
#define TREAPBASEITERATOR_H

#include "basetypes.h"
/*
namespace DataStructures {

  template <typename T>
  class TreapBaseIterator : public std::iterator<std::random_access_iterator_tag, T, difference_type>
  {
  public:
    TreapBaseIterator(index_type index = 0);
    TreapBaseIterator(const TreapBaseIterator<T>& other);
    difference_type operator-(const TreapBaseIterator<T>& other) const;
    bool operator==(const TreapBaseIterator<T>& other) const;
    bool operator!=(const TreapBaseIterator<T>& other) const;
    bool operator>=(const TreapBaseIterator<T>& other) const;
    bool operator<=(const TreapBaseIterator<T>& other) const;
    bool operator>(const TreapBaseIterator<T>& other) const;
    bool operator<(const TreapBaseIterator<T>& other) const;
  protected:
    index_type m_index;
  };

}*/

#endif // TREAPBASEITERATOR_H
